# ELLA Azure Deployment - Optimized Requirements
# Core Web Framework
fastapi==0.95.2
uvicorn[standard]==0.24.0.post1
gunicorn==21.2.0

# Essential Dependencies Only
requests==2.32.3
python-dotenv==1.1.0
pydantic==1.10.22

# Database - Core Only
redis==5.3.0
pymongo==4.13.0

# AI - Essential Only
openai==1.81.0
langchain-openai==0.1.25
langchain-core==0.2.40
langchain==0.2.16

# File Processing - Lightweight
pillow==11.2.1
aiofiles==0.8.0

# Utilities
python-dateutil==2.9.0.post0
tenacity==8.5.0

# Additional essentials for your app
python-multipart==0.0.20
httpx==0.28.1
jinja2==3.1.6
uuid6==2024.7.10
flask==3.0.0

# Remove heavy dependencies that cause build timeouts:
# - torch (2.7GB+ download)
# - transformers (500MB+ with models)
# - scikit-learn (large compiled dependencies)
# - scipy (large compiled dependencies)
# - sentence-transformers (depends on torch)
# - boto3 (if not using AWS)

# Note: If you need AI features, consider:
# 1. Using Azure OpenAI Service instead of local models
# 2. Pre-building wheels and using private PyPI
# 3. Using Azure Container Instances for heavy ML workloads 